SET(LIB_NAME cspsim)

SET (SOURCE_AND_HEADER_PATH ${csp_SOURCE_DIR}/csp/cspsim)
MESSAGE("${csp_SOURCE_DIR}")
MESSAGE("${SOURCE_AND_HEADER_PATH}")

ADD_LIBRARY(${LIB_NAME} SHARED 
	${SOURCE_AND_HEADER_PATH}/CSPSim.h 
	${SOURCE_AND_HEADER_PATH}/CSPSim.cpp
)